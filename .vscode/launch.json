{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "gdb",
        "request": "launch",
        "name": "Launch Program",
        "target": "${workspaceFolder}/build/bin/demo",
        "cwd": "${workspaceFolder}",
        "valuesFormatting": "parseText",
        "preLaunchTask": "Build"
      }
    ]
}
